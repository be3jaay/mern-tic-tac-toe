// import { useCallback } from "react";

// export const useWindowDirty = (
//   enabled: boolean,
//   message: "Are you sure you want to exit? Changes you made may not be saved."
// ) => {

//   const handler = useCallback(e: Event)
//   useEffect(() => {
//     if (enabled) {
//       window.addEventListener("beforeunload", handleBeforeUnload);
//     }
//   }, [enabled]);
// }
